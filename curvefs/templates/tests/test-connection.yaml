apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "curvefs.fullname" . }}-test-connection"
  labels:
    {{- include "curvefs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "curvefs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
